apiVersion: 1

providers:
  - name: warehouse
    orgId: 1
    type: file
    folder: warehouse
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
